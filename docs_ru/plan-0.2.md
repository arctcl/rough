# План v0.2 — надёжность (Windows) и улучшения движка

> Статус: **в работе**. Сделано (2026-08-27):
> - `engine/faultlog` (GoSafe, crash.log+rough.log, MemStats, InitSignals), подключён в `engine.go`;
> - флаг `--tui-debug` (пауза при краше);
> - **перезапуск экрана при панике** в чтении событий (вместо выхода);
> - снапшот-точка отката (коммит `124d11c`);
> - кукингбук синтаксиса `docs_ru/cookbook-syntax.md`;
> - **CI** (`.github/workflows/ci.yml`: build+test+vet+`-race` на Linux и Windows);
> - **все фиксы аудита F1–F7**;
> - **фича `var%pat`** (Bash Parameter Expansion) + тесты + доки;
> - **рефакторинг шаг 1**: состояние мыши вынесено в `Engine` (`engine_state.go`).
> Проверки: `go build`/`go test ./...`/`go vet` чистые, всё закоммичено.
>
> v0.2 = надёжность (Windows-вылеты, отлов) + **фиксы из аудита** +
> **рефакторинг глобалов** + **CI**. Каждый пункт — маленький, тестируемый,
> своим коммитом.

---

## Часть 1. Почему Windows вылетает «без логов» и что делать

### 1.1 Установленные по коду причины (диагноз)

1. **recover в `Run` ловит панику ТОЛЬКО в горутине main.**
   В `engine/engine.go` два фоновых потока запускаются через `go func(){...}()`
   **без какого-либо recover**:
   - пул событий: `go func(){ for { evCh <- s.PollEvent() } }()` — главный подозреваемый;
   - телеграфная мышь: `go func(){ ... tm.read(...) ... }()` (актуально на Linux, но
     код должен быть защищён везде).
   Если `s.PollEvent()` паникует внутри tcell (Windows-консоль, ресайз, быстрый
   ввод, мёртвые/составные клавиши, сброс терминала), паника уходит в **эту**
   горутину, процесс умирает, а `recover` из `Run` **не срабатывает** → `crash.log`
   не пишется. Это и есть «молчаливый вылет».

2. **Верхнеуровневый перехват в `Run` — единственный.**
   Логика: «написали в crash.log, вернули ошибку». Но он не покрывает: чужие
   горутины, `os.Exit`/`runtime.Goexit`, аппаратные сбои (segfault), нехватку
   памяти (OOM/выделение на лету в пайпах).

3. **Нет структурированного лога и снятия состояния.**
   Нет `os/signal` (нет грейсфул-шатдауна, нет корректного `s.Fini()` при
   Ctrl+C/SIGTERM), нет `runtime.ReadMemStats` (нет метрик памяти перед крашем),
   нет ротации `crash.log` (перезаписывается, теряется история).

### 1.2 Фреймворк отлова и диагностики (новый)

Предлагаю отдельный пакет `engine/faultlog` (изолированный, без зависимостей
движка, тестируемый): «фрейм, внутри которого отлавливаются память/логи/стек».

**A. Защищённый запуск горутин `GoSafe(name string, fn func())`**
- Оборачивает `go fn()` в `defer recover` → при панике пишет полный дамп
  (значение паники + `debug.Stack()` + дамп ВСЕХ горутин через
  `pprof.Lookup("goroutine").WriteTo`) и помечает движок «упавшим».
- В `Run` заменить оба `go func(){...}()` на `faultlog.GoSafe("poll", ...)`
  и `faultlog.GoSafe("teletype", ...)`.
- Опционально: сторожевой таймер — если горутина событий умерла (канал закрыт),
  логируем и пробуем пересоздать экран/цикл, а не падать.

**B. Глобальный обработчик сигналов `InitSignals(onExit func())`**
- `os/signal.Notify(ch, os.Interrupt, syscall.SIGTERM)`.
- По сигналу: корректно `s.Fini()`, флашим лог, возвращаем код выхода.
- На Windows `os.Interrupt` = Ctrl+C; `SIGTERM` в `golang.org/x/sys/windows` не
  поддерживается — обрабатываем только то, что реально приходит.

**C. Структурированный лог `faultlog.Log` (crash.log + rough.log)**
- Два файла:
  - `crash.log` — только фатальные события (перезаписывается, как сейчас, но с
    таймстампом и стеком всех горутин);
  - `rough.log` — кольцевой (append, максимум N байт): уровень, время, событие,
    метрики памяти — видно историю перед крашем.
- Метрики памяти при каждом логируемом событии и на выходе:
  `runtime.ReadMemStats`: `HeapAlloc`, `HeapSys`, `NumGC`, `NumGoroutine`,
  `LastGC`.

**D. Флаг `--tui-debug`**
- `--tui-debug` включает: подробный лог в `rough.log`, и на панике — **паузу**:
  терминал НЕ закрывается сразу, вверху рисуем/печатаем «crash, жми Enter»,
  чтобы можно было подцепить отладчик к живому процессу и снять стек/состояние
  до того, как всё схлопнется.

---

## Часть 2. Улучшения движка

### 2.1 Переменные (единственная фича из плана)
- ✅ **Bash Parameter Expansion (`var%pat`)** — расширение подстановки переменных
  в `engine/vars.go`: `${var%pat}` / `${var#pat}` (усечение суффикса/префикса),
  как в bash. Тесты в `testing_vars_test.go`, доки в `cookbook-syntax.md` §8.
- Примечание: `loop` (перебор диапазонов) НЕ трогаем — в v0.2 он вне плана.

### 2.2 Надёжность рантайма
- ✅ **Перезапуск tcell-экрана при ошибке** вместо смерти: если `PollEvent` упал,
  пробуем `s.Fini()` + `s.Init()` заново и продолжаем (иначе — ошибка с дампом).
- **Защита от гонок**: пометить кандидатов (глобалы `hotzones`, `outputCache`,
  `stateCache`, `curTheme`) — на будущее запуск с `-race` в CI (Linux).
- **Ограничение вложенности/размера**: защита от глубокой рекурсии HTML и от
  бесконечных пайпов (лимит шагов на действие), чтобы «зависание» не выглядело
  как вылет.

### 2.3 UX / мелочи
- Статус-окно: многострочные ошибки не обрезать молча (скролл уже есть —
  проверить переполнение).
- Примечание: колёсико мыши для прокрутки УЖЕ работает — вне плана.

### 2.4 Фиксы из аудита (2026-08-27) — ВЫПОЛНЕНЫ

- ✅ **F1. Недетерминизм `pattern`** в `engine/loader_tiles.go`: `pattern` теперь
  читается ДО цикла по страницам (порядок map больше не влияет).
- ✅ **F2. Дыра в `$подстановке`**: `expandVars` теперь уважает одинарные кавычки
  (`'...'` — литерал, без подстановки) — поле ввода не может прочитать переменную.
- ✅ **F3. Паники**: защита от `b.W==0` в `renderImg` (деление на ноль) и от
  переполнения `w*h*3` в `decodePPM` (+ пропуск всех пробелов перед данными).
- ✅ **F4. Утечка**: `pluginCache` ограничен (`maxPluginCache=256`, сброс при переполнении).
- ✅ **F5. `renderTile`**: запас высоты увеличен с 4× до 16× (мин 200, потолок 4000).
- ✅ **F6. `faultlog`**: добавлен мьютекс на запись логов (нет порчи `rough.log`).
- ✅ **F7. Мелкие**: `forbiddenName` регистро-независим (`EqualFold`); `LoadTheme`
  логирует ошибки JSON; `scrollOff` ключуется по `route+id`, а не только id.
  ключ `pattern` и страницы в одном `map`, порядок обхода случайный. Если
  `pattern` встретится после страниц — те распарсятся стандартным паттерном.
  Ломает проекты с нестандартным порядком полей. Фикс: читать `pattern` ДО
  цикла по страницам.
- **F2. Дыра в `$подстановке`**: `expandVars` (`engine/vars.go`) не уважает
  кавычки, а значение переменной вставляется без пере-экранирования → `:`/`'`
  в значении режут аргументы (инъекция аргументов, в т.ч. в ssh). Фикс:
  уважать кавычки в expandVars + пере-экранировать подставленное значение.
- **F3. Два пути к панике**: деление на ноль в `renderImg` (`img.w / b.W` при
  ширине тайла 1–2 клетки); переполнение `w*h*3` в `decodePPM`. Фикс: защита
  от `b.W==0` и от переполнения.
- **F4. Утечка памяти**: `pluginCache` не вытесняется при динамических пайпах
  (ключ после `expandVars`), `outputCache` растёт при склейке `clear && …`.
  Фикс: вытеснение/лимит.
- **F5. `renderTile` обрезает контент на `4×viewH`**: хвост длинного вывода
  недостижим, полоса прокрутки «врёт». Фикс: честный расчёт высоты.
- **F6. `faultlog` без мьютекса**: `AppendLog`/`WriteCrash`/`trimHistory`
  вызываются из нескольких горутин → риск порчи `rough.log`. Фикс: мьютекс.
- **F7. Мелкие**: `LoadTheme` игнорирует JSON-ошибки (тихая пустая тема);
  `decodePPM` off-by-one при `\r\n`; `forbiddenName="run"` регистро-зависим;
  `scrollOff` ключуется по `id` тайла, а не роуту.

### 2.5 Рефакторинг: упаковать глобалы в `Engine`

- **ШАГ 1 ГОТОВ**: состояние мыши (`mouseX/Y`, `mouseBtn1`, `mouseLastX/Y`)
  вынесено в `Engine` (`engine_state.go`, `eng`). Функции читают `eng.*`.
- Осталось: перенести в `Engine` остальное — `statusMsg/statusShownAt/statusScroll`,
  режимы `input/select/confirm`, `focusIdx`, `hotzones`, `outputCache`,
  `stateCache`, `curTheme` и др.; конвертировать обработчики в методы `(e *Engine)`.
- Зачем: сейчас десятки глобалов в ~10 файлах, переплетённых между собой —
  будущая конкурентность = гонки, которые без `-race` не поймать.
- Риск: **высокий** (трогает почти каждый файл). Идём этапами (шаг 1 сделан),
  с проверкой `go build`+`go test` после каждого этапа.

### 2.6 CI (GitHub Actions)

- Workflow на каждый push/PR: `go build ./...`, `go test ./...`, `go vet ./...`
  + `go test -race ./...` на Linux (там gcc есть — ловим гонки).
- Зачем: страховка от регрессий, особенно ВО ВРЕМЯ рефакторинга (п.2.5).

---

## Часть 3. Порядок работ и контроль качества

### Порядок (маленькие шаги, каждый — свой коммит)
1. ✅ **faultlog** (пакет + тесты): `GoSafe`, `Log`, `ReadMemStats`-снимок, ротация.
2. ✅ Подключить `GoSafe` в оба `go func` в `engine.go`.
3. ✅ `InitSignals` + грейсфул `s.Fini()`.
4. ✅ Флаг `--tui-debug` + пауза при краше.
5. ✅ Перезапуск экрана при панике в чтении событий (вместо выхода).
6. ✅ **CI** (GitHub Actions): build+test+vet+`-race` на Linux и Windows.
7. ⏳ Демо-проверка на Windows (20+ мин: ресайз/ввод/мышь — больше не «молчит»).
8. ✅ **Фиксы аудита F1–F7** (см. §2.4).
9. ✅ Bash Parameter Expansion `%`/`#` в `vars.go` + тесты.
10. ✅ Рефакторинг — ШАГ 1 (состояние мыши в `Engine`).
11. ⏳ Рефакторинг — ШАГИ 2+ (статус/режимы/focus/hotzones/outputCache/curTheme
    в `Engine`, обработчики → методы).
12. ⏳ UX-мелочи (ошибки в статусе) + регресс.
11. ⏳ UX-мелочи (ошибки в статусе) + регресс.

### Критерии приёмки
- На Windows-терминале 20+ минут активной работы (ресайз, ввод, мышь, графика)
  без единого молчаливого вылета; при любом падении — есть дамп в `crash.log` +
  история в `rough.log`.
- `go test ./...` зелёный, `go vet` чистый, `go test -race` (Linux) чистый.
- Новая фича `var%pat` — со справкой `man` и тестами, в агрегаторе.
- Рефакторинг глобалов — без регрессий (под CI-страховкой), поэтапно.

### Вне объёма (на будущее)
- Сторожевой подпроцесс-вотчер (полный watchdog с перезапуском извне).
- Сетевой pprof-эндпоинт (движок принципиально без портов — не вводим).
